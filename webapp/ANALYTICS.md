# Analytics

Analytics is disabled in this starter. No PostHog key is included and `analyticsEnabled` always returns false. The event utilities remain available for a downstream project to opt in with its own provider, host policy and privacy notice. Never restore another deployment's project token.
